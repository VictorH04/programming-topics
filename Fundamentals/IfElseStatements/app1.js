let firstName = 'Victor';
let civilStatus = 'single';

if (civilStatus === 'married') {
	console.log(firstName + ' Is married!');
} else {
	console.log(firstName + ' Will hopefully marry soon');
}

let isMarried = true;
if (isMarried) {
	console.log(firstName + ' is married!');
} else {
	console.log(firstName + ' will hopefully marry soon sier vi');
}
