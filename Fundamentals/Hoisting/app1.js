// Functions

calculateAge(1965);

function calculateAge(year) {
	console.log(2016 - year);
}

// retirement(1965);
let retirement = function(year) {
	console.log(65 - (2016 - year));
};

// Variables
let age = 23;
console.log(age);

foo();
function foo() {
	let age = 65;
	console.log(age);
}
